#include <iostream>

#include <move.h>
#include <util.h>

using namespace Cheddar;

int main() {
    std::cout << "Cheddar's command-line interface is a work in progress." << std::endl;

    return 0;
}
